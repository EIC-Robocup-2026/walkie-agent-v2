"""Self-contained CLI utilities for the walkie_graphs store.

Separate from the repo's legacy ``tools/`` (which target the old chroma_db). Run
with the robot stopped — ChromaDB's persistent client is single-process.
"""
