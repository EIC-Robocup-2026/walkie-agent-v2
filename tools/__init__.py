"""Developer-facing tools that are not part of the on-robot runtime.

Currently houses ``chroma_viewer`` — a read-only Flask web UI for
inspecting the ChromaDB stores this project writes (objects, people,
scenes, and the CLIP ``scene_entries`` memory).
"""
